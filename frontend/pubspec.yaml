name: extensao_frontend
description: A new Flutter project for extensao.

environment:
  sdk: '^3.7.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.3.0
  shared_preferences: ^2.5.3
  provider: ^6.0.5
  flutter_secure_storage: ^8.0.0
  intl: ^0.18.1
  connectivity_plus: ^4.0.2
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.2

flutter:
  uses-material-design: true
  assets:
    - assets/images/

