name: extensao_frontend  # Changed from cmei_app
description: App para comunicação entre CMEI e famílias.

environment:
  sdk: '^3.7.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  sqflite: ^2.3.0
  sqflite_common_ffi: ^2.3.0
  path_provider: ^2.1.1
  flutter_secure_storage: ^8.0.0
  connectivity_plus: ^4.0.2
  get_it: ^7.6.0
  path: ^1.8.3
  provider: ^6.0.5
  device_preview: ^1.1.0
  intl: 0.20.2
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  mockito: ^5.4.2
  build_runner: ^2.4.6

flutter:
  uses-material-design: true
  assets:
    - assets/images/

